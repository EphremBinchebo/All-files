EngageAI Backend (sample)

- Run with Maven: mvn clean package && java -jar target/engageai-backend-1.0.0.jar
- application.yml contains stripe.secret (set to your test secret key)
- CORS allowed-origin is set; replace with your frontend URL
