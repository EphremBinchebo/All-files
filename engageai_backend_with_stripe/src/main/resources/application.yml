server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/engageai_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: example
  jpa:
    hibernate:
      ddl-auto: update

stripe:
  secret: sk_test_YOUR_TEST_KEY

cors:
  allowed-origin: https://app.engageai.example  # replace with your frontend remote URL
