EngageAI Frontend (remote example)

1. Set real backend base URL in src/services/api.js -> BASE_URL
2. Install dependencies: npm install
3. Run dev server: npm run dev
4. Pages: /login, / (dashboard), /payments
